DECLARE @counter INT 
SELECT @counter = 20

-- Create a loop
WHILE @counter >= 1

-- Loop code starting point
BEGIN
    PRINT REPLICATE('* ', @counter)
    SET @counter = @counter - 1
-- Loop finish
END
